# Generated at container start by docker-entrypoint.sh
# Do not use {$PORT} or {$SERVER_NAME} here — Railway may inject broken values.
# See docker-entrypoint.sh for the live config.

{
	admin off
	persist_config off
	auto_https off

	frankenphp
	order php_server before file_server
}

:8080 {
	root * /app
	encode gzip

	php_server
	file_server
}
